namespace VipExtraction
{
    // This harmless marker forces Unity to rebuild after the stash scripts
    // are replaced while the Editor is already open.
    internal static class StashCompileRefresh
    {
    }
}
